---
all:
  children:
    frontend:
      hosts:
        frontend-node:
          ansible_connection: local
          ansible_user: ubuntu
    
    backend:
      hosts:
        backend-node:
          ansible_connection: local
          ansible_user: ubuntu
